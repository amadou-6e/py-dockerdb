{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Win Python: Current File",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      "env": {
        "PYTHONPATH": "${workspaceFolder}/docker_db/src;${workspaceFolder}/docker_db/;${PYTHONPATH}"
      }
    },
    {
      "name": "Lin Python: Current File",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      "env": {
        "PYTHONPATH": "${workspaceFolder}/docker_db/src:${workspaceFolder}/docker_db/:${PYTHONPATH}"
      }
    }
  ]
}